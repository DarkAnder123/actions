name: Shell Commands
on:
  - push
jobs:
  run-shell-commands:
    runs-on: ubuntu-latest
    steps:
      - name: echo Hello
        run: echo "Hello world"
      - name: version
        run: |
          node -v
          npm -v
