name: Matrix
on: push
jobs:
  node-version:
    runs-on: ubuntu-latest
    steps:
      - name: Node Version
        run: node -v
      - uses: actions/setup-node@v2
        with:
          node-version: '16'
